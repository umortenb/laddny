import {
  accentColor,
  errorColor,
  neutralColor,
  primaryColor,
} from "./colorPalettes";

export const mainTheme = {
  primaryColor: primaryColor,
  neutralColor: neutralColor,
  accentColor: accentColor,
  errorColor: errorColor,
  backgroundColor: primaryColor[100],
  headerBackgroundColor: primaryColor[100],
};
